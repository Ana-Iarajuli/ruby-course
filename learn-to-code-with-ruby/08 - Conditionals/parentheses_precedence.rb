def agent(num, name, job)
    if (num == "007" && name == "ana") || job == "Secret Agent"
        job = "Secret Agent"
        puts job
    else
        puts "Bandzi"
    end

end

agent("007", "aa", "teacher")